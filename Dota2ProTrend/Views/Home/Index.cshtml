@model IEnumerable<Dota2ProTrend.Models.Hero>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(heroes => heroes.heronumber)
        </th>
        <th>
            @Html.DisplayNameFor(heroes => heroes.heroname)
        </th>
        <th>
            
        </th>
        <th>
           
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(heroItem => item.heronumber)
        </td>
        <td>
            @Html.DisplayFor(heroItem => item.heroname)
        </td>
        <td>
           
        </td>
        <td>
           
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
